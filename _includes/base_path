{% comment %}
  Always use relative root paths to avoid host mismatches in dev.
  GitHub Pages serves the site at the domain root, so `site.baseurl` is sufficient.
{% endcomment %}
{% assign base_path = site.baseurl %}
